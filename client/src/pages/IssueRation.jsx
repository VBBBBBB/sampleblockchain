import { Box, Typography } from '@mui/material';

const IssueRation = () => (
    <Box p={3}>
        <Typography variant="h4">Ration Distribution Point</Typography>
        <Typography variant="body1" mt={2}>Use the POS terminal to issue ration to citizens.</Typography>
    </Box>
);
export default IssueRation;
